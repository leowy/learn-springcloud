server:
    port: 9080
spring:
    application:
        name: hystrix-dashboard
management:
    endpoints:
        web:
            exposure:
                include: '*'
eureka:
    client:
        service-url:
            defaultZone: http://localhost:8081/eureka/
